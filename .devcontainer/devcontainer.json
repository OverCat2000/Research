{
    "postCreateCommand": "pip install -r requirements.txt", // Automatically install dependencies
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python" // Installs the Python extension
            ]
        }
    }
}